import { Example10PipesPipe } from './example10-pipes.pipe';

describe('Example10PipesPipe', () => {
  it('create an instance', () => {
    const pipe = new Example10PipesPipe();
    expect(pipe).toBeTruthy();
  });
});
